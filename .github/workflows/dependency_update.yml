name: Dependency Update
on:
  schedule:
    - cron: '0 1 * * *'
jobs:
  deps:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: pip install pip-tools
      - run: echo "TODO: compile updated requirements"
