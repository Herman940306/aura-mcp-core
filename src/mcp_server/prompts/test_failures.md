# /test_failures

Analyze failing tests and produce:

- Failure categorization
- Suspected root causes
- Minimal, reversible fixes
- Follow-up validation steps

Inputs: test report (stdout/JSON), recent changes
